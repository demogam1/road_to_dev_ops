# Project To-Do List

## Tasks

-   Define the database structure
-   Sensitive information must be obfuscated using Docker Secrets
-   Create a Docker Compose file to launch the entire app (frontend,
    backend, database)
-   Create docker compose that links db to the backend
-   Configure postresql
-   delete backend/dockerfile.dev
-   Configurer mangoDB